# 招投标智能问答系统 — 前端使用指南

> 前端地址：http://localhost:3000
> 后端 API：http://localhost:8001 （需同时运行）

---

## 一、界面总览

```
┌──────────────────────────────────────────────────┐
│  [侧边栏]         │       [主区域]                │
│  🔵 新建对话       │       [模型选择] DeepSeek ▼  │
│  🌙 深色模式切换   │                              │
│  🔍 搜索历史       │   ┌─────────────────────┐   │
│                    │   │                     │   │
│  对话列表          │   │    问答消息流        │   │
│  ├ 什么是单一来源  │   │   (用户消息 + AI回答) │   │
│  ├ 空调采购有哪些  │   │                     │   │
│  └ ...             │   │                     │   │
│                    │   └─────────────────────┘   │
│                    │                              │
│                    │   ┌─────────────────────┐   │
│                    │   │ 📎 🌐 🌊  [发送]     │   │
│                    │   │  输入框              │   │
│                    │   └─────────────────────┘   │
└──────────────────────────────────────────────────┘
```

---

## 二、快速开始（3 步上手）

### 第 1 步：启动服务

```bash
# 方式 A：双击 start.bat
# 方式 B：分别运行
uv run python main.py api    # 后端  http://localhost:8001
cd frontend && npm run dev   # 前端  http://localhost:3000
```

### 第 2 步：打开浏览器

访问 **http://localhost:3000**

### 第 3 步：选择问题发送

首页展示 6 个示例问题，**直接点击即可发送**：

| 示例问题 | 触发的数据源 |
|---|---|
| 什么是单一来源采购？ | 📚 知识库 RAG |
| 有哪些关于空调的采购？ | 🗄️ PostgreSQL 数据库 |
| 采购最多的标的是什么？ | 🗄️ PostgreSQL + 📈 聚合 |
| 空调的供应商和采购人有哪些？ | 🕸️ 知识图谱 Neo4j |
| 对比中国大陆和香港招投标流程法规 | 🌐 联网搜索 + 📚 知识库 |
| 知识图谱中有多少数据？ | 🕸️ Neo4j 统计 |

---

## 三、输入框详解

```
┌────────────────────────────────────────────┐
│                                            │
│  输入招投标问题...                         │
│                                            │
├────────────────────────────────────────────┤
│ 📎  🌐联网搜索  🧠深度思考      [发送] ➤  │
└────────────────────────────────────────────┘
```

### 快捷键

| 操作 | 快捷键 |
|---|---|
| 发送消息 | **Enter** |
| 换行（不发送） | **Shift + Enter** |

### 输入方式

| 方式 | 说明 |
|---|---|
| **纯文本问题** | 直接输入，Enter 发送 |
| **图片 + 文字** | 先点 📎 上传图片，再输入问题文字 |
| **纯图片分析** | 只上传图片（不需要输入文字），AI 自动识别图片内容 |

### 功能开关

| 按钮 | 效果 | 什么时候用 |
|---|---|---|
| **🌐 联网搜索** | AI 额外调用 Tavily/Exa 搜索互联网 | 需要最新信息、法规对比、实时数据时。回答底部会显示网页来源卡片 |
| **🧠 深度思考** | AI 用更强的推理模型思考，回答更详细 | 复杂问题、需要多步推理时。回答中会出现可折叠的**紫色"思考过程"**块 |

> 💡 两个开关的状态会自动保存到浏览器 localStorage，刷新页面不丢失。

### 图片上传限制

| 限制 | 值 |
|---|---|
| 文件大小 | 最大 **10 MB** |
| 支持格式 | 所有浏览器能识别的图片（jpg/png/webp...） |
| 自动压缩 | 超过 1024px 自动缩小、JPEG 压缩 0.85、透明区域铺白底 |
| 处理方式 | base64 编码 → 后端 Vision 接口（glm-4v-flashx） |

---

## 四、AI 回答解读

### 回答结构

AI 回答一条完整消息可能包含以下部分（按顺序）：

```
┌─────────────────────────────────┐
│ 🛠️ 已搜索知识库                  │ ← 工具来源标签（灰色圆角）
├─────────────────────────────────┤
│ ▶ 思考过程 (可折叠)              │ ← 紫色，仅"深度思考"模式出现
│   用户问的是...                   │
│   我需要查...                     │
├─────────────────────────────────┤
│                                 │
│   **AI 回答正文**（Markdown）     │ ← 支持表格、代码、列表、链接
│                                 │
├─────────────────────────────────┤
│ 📚 知识库来源 (相似度 92%)        │ ← 来源卡片 1
│    Q: 什么是公开招标...           │
│    A: 公开招标是指...             │
├─────────────────────────────────┤
│ 📚 知识库来源 (相似度 78%)        │ ← 来源卡片 2
│    ...                           │
├─────────────────────────────────┤
│ [复制] [🔄 重新生成] [👍] [👎] 3.2s │ ← 底部操作 + 用时
└─────────────────────────────────┘
```

### 工具来源标签

AI 会自动选择一个或多个工具，标签会告诉你用了什么：

| 标签 | 含义 |
|---|---|
| 🛠️ 已搜索知识库 | Qdrant 向量检索 + BM25 稀疏匹配 |
| 🛠️ 已查询知识图谱 | Neo4j 图数据库查询 |
| 🛠️ 已查询数据库 | PostgreSQL 结构化 SQL 查询 |
| 🛠️ 已联网搜索 | Tavily / Exa 搜索互联网 |
| 🛠️ 已语义搜索 | Exa 语义搜索引擎 |

### 来源卡片

回答底部显示 AI 参考的来源（最多 5 张卡片）：

| 字段 | 说明 |
|---|---|
| 相似度百分比 | 绿色 ≥ 70% / 黄色 ≥ 40% / 灰色 < 40% |
| 问题 | 匹配到的知识库原始问题 |
| 摘要 | 答案摘录（最多 3 行） |
| 打开链接 | 联网搜索的来源可点击跳转 |

### 底部操作按钮

| 按钮 | 功能 |
|---|---|
| 📋 复制 | 一键复制 AI 回答到剪贴板（兼容无 Clipboard API 的浏览器） |
| 🔄 重新生成 | 删掉上一轮问题，让 AI 用相同参数重新回答（每次回答略有不同） |
| 👍 满意 | 正面反馈，保存到 PostgreSQL feedback 表 |
| 👎 不满意 | 负面反馈，帮助团队识别回答质量问题 |
| **3.2s** | 回答生成耗时（含检索 + LLM 推理） |

### 流式输出行为

| 场景 | 表现 |
|---|---|
| 正常回答 | 文字逐字"打字机"式出现，视觉上 20ms/40 字符节奏 |
| AI 调用工具 | 会出现短暂停（检索中...），然后继续输出 |
| 用户中途停止 | 点击浏览器"停止"或切换对话，已渲染的文字保留不丢失 |
| 网络断连 | 回答末尾追加 `⚠️ 回答可能不完整`，提示用户重试 |

---

## 五、侧边栏：对话历史管理

### 模型选择（顶部下拉）

| 选项 | Provider | 适用场景 |
|---|---|---|
| DeepSeek | deepseek-v4-flash | **默认推荐**，平衡速度和质量 |
| 智谱AI | glm-4-flash | 中文能力好 |
| vLLM | 本地部署 | 私有化，需要自己跑 vLLM 服务 |
| Ollama | 本地部署 | 完全离线，需要自己装 Ollama + 模型 |

> 💡 选择会自动保存到 localStorage，下次打开还是同样的。

### 侧边栏功能

```
┌─ 侧边栏 ─────────────────────┐
│ [🔵 新建对话]  [🌙] [✕]      │  ← 新建 / 深色模式 / 移动端关闭
│ [🔍 搜索历史对话]             │  ← 300ms 防抖实时搜索
│                               │
│ 什么是单一来源采购？           │  ← 对话列表
│ 有哪些关于空调的采购？         │     当前选中 = 蓝色高亮
│ [⋯] 空调供应商和采购人         │     悬浮显示 [⋯] 更多菜单
│                               │
│      [删除所有对话]            │  ← 底部红色按钮（有二次确认）
└───────────────────────────────┘
```

| 功能 | 操作 |
|---|---|
| **新建对话** | 点击 🔵 按钮，立即开始新会话 |
| **切换对话** | 点击列表中的对话标题 |
| **搜索对话** | 输入框打字，300ms 后过滤 |
| **删除单条** | 悬浮对话 → 点击 ⋯ → 删除 |
| **删除全部** | 底部红色按钮 → 弹窗确认 |
| **深色模式** | 点击 🌙 按钮，全局切换，刷新保留 |
| **移动端** | 屏幕 < md 时，侧边栏默认隐藏；汉堡菜单打开 |

### 会话持久化

| 维度 | 说明 |
|---|---|
| 存储位置 | PostgreSQL `conversations` 表 |
| 保存时机 | 每次 AI 回答完成后自动保存 |
| 标题 | 取用户第一条问题（或图片名） |
| 跨设备 | 不支持（目前是单机本地数据库） |
| 恢复 | 刷新页面 → 自动加载历史列表 → 点击恢复 |

---

## 六、常见使用场景

### 场景 1：快速查知识问答

```
输入：什么是单一来源采购？
→ AI 自动检索知识库，返回法规定义
→ 底部显示来源卡片（知识库里的原始问答）
```

### 场景 2：查结构化数据

```
输入：采购最多的标的是什么？
→ AI 调用 PostgreSQL 做 COUNT 聚合
→ 返回 TOP N 统计结果
```

### 场景 3：查供应商关系

```
输入：空调的供应商和采购人有哪些？
→ AI 查询 Neo4j 知识图谱
→ 返回图结构的关系列表
```

### 场景 4：对比不同法规

```
开启：🌐 联网搜索
输入：对比中国大陆和香港招投标流程法规
→ AI 同时查知识库 + 联网搜索
→ 返回对比表格，底部显示网页来源链接
```

### 场景 5：分析上传的招标文件

```
1. 点击 📎 上传招标文件截图/图片
2. （可选）输入分析要求
→ AI 用视觉模型识别图片内容
→ 返回结构化解读
```

### 场景 6：多轮对话（延续上下文）

```
第 1 轮：什么是公开招标？
第 2 轮：那它和邀请招标有什么区别？    ← "它"自动指代"公开招标"
第 3 轮：能举个例子吗？                  ← AI 知道上下文
→ 对话历史自动截取最近 5 轮，早期消息做字符串压缩
```

---

## 七、FAQ

### Q: AI 回答很慢怎么办？

| 可能原因 | 解决 |
|---|---|
| 开启了 🧠 深度思考 | 关掉它，普通模式更快 |
| 开启了 🌐 联网搜索 | 关掉它，纯知识库检索更快 |
| 首次提问有模型预热延迟 | BGE-M3 + reranker 首次调用会加载，之后就快了 |
| 后端没启动 | 检查 http://localhost:8001/api/health 是否 ready=true |

### Q: 回答引用的来源是什么意思？

底部来源卡片是 AI **实际用来生成回答的参考内容**。相似度表示该来源与你的问题的匹配程度。点开可以看到原始问答内容。

### Q: "重新生成"和"再问一遍"有什么区别？

| 操作 | 区别 |
|---|---|
| 🔄 重新生成 | 删掉上一轮问题，**用完全相同的参数**重新调用 AI |
| 手动输入再问 | 会累积到对话历史，**成为多轮上下文的一部分** |

### Q: 为什么有时消息消失了？

切换对话、删除对话、或新建对话时，当前内存中的消息会被新会话替换。只要 AI 回答完成过（数据库已保存），切回来就能恢复。

### Q: 深色模式怎么永久生效？

自动保存到浏览器 localStorage。换浏览器或清缓存后需要重新设置。

### Q: 移动端能用吗？

能用。屏幕宽度 < md（约 768px）时：
- 侧边栏隐藏，汉堡菜单打开
- 输入框自动换行

---

## 八、故障排查

| 现象 | 检查 |
|---|---|
| 页面空白 / 加载中 | 后端是否启动？访问 http://localhost:8001/api/health |
| 发送后报错 "连接中断" | 检查后端日志，看是不是 PG/Neo4j 超时 |
| AI 不回答 / 转圈 | 模型 API Key 是否配置？检查 `.env` 文件 |
| 搜索历史没结果 | PostgreSQL 是否在运行？history 存在 conversations 表 |
| 图片上传失败 | 是否超过 10MB？是否是浏览器支持的格式？ |
| 回答被截断 | 可能是 `MAX_TOOL_ROUNDS=4` 到上限，或流式缓冲异常 |
| GPU 温度太高 | 打开 http://localhost:8001/api/system/metrics 看实时监控 |

---

## 九、相关地址速查

| 服务 | 地址 |
|---|---|
| **前端页面** | http://localhost:3000 |
| **后端 API 文档** | http://localhost:8001/docs |
| **健康检查** | http://localhost:8001/api/health |
| **系统监控** | http://localhost:8001/api/system/metrics |
| **一键启动** | `双击 start.bat` |
| **一键停止** | `双击 stop.bat` |
| **GitHub 仓库** | https://github.com/wuyang-vs/Bidding_QA_Chatbot |
